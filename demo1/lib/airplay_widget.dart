import 'package:flutter/material.dart';

class AirPlayWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: Text("AIRPLAY"),
      ),
      body: Center(
        child: Text("airplay"),
      ),
    );
  }
}
